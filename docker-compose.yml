services:
  aerospike:
    image: docker.io/aerospike/aerospike-server:latest
    ports:
      - "3100:3100"
      - "3101:3101"
      - "3102:3102"
    volumes:
      - aerospike-data:/opt/aerospike/data:Z
      - ./aerospike.conf:/opt/aerospike/etc/aerospike.conf:Z
    command: ["--config-file", "/opt/aerospike/etc/aerospike.conf"]
    networks:
      - merkle-net

  zookeeper:
    image: docker.io/confluentinc/cp-zookeeper:7.6.0
    environment:
      ZOOKEEPER_CLIENT_PORT: "2181"
      ZOOKEEPER_TICK_TIME: "2000"
    ports:
      - "2181:2181"
    networks:
      - merkle-net

  kafka:
    image: docker.io/confluentinc/cp-kafka:7.6.0
    depends_on:
      - zookeeper
    ports:
      - "9192:9192"
    environment:
      KAFKA_BROKER_ID: "1"
      KAFKA_ZOOKEEPER_CONNECT: "zookeeper:2181"
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: "PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT"
      KAFKA_ADVERTISED_LISTENERS: "PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:9192"
      KAFKA_LISTENERS: "PLAINTEXT://0.0.0.0:29092,PLAINTEXT_HOST://0.0.0.0:9192"
      KAFKA_INTER_BROKER_LISTENER_NAME: "PLAINTEXT"
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: "1"
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
    networks:
      - merkle-net

networks:
  merkle-net:
    driver: bridge

volumes:
  aerospike-data:
